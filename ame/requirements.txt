# AME - Another Me Engine Dependencies
# Version: 0.2.0

# Core dependencies
numpy>=1.24.0,<2.0.0
openai>=1.0.0
pydantic>=2.0.0  # Data validation

# Vector Store (Faiss)
faiss-cpu>=1.7.4  # CPU version (use faiss-gpu for GPU support)

# Graph Database (FalkorDB)
falkordb==1.0.8
redis>=5.0.1  # FalkorDB backend

# NER & NLP (for entity extraction)
jieba>=0.42.1  # Chinese word segmentation
paddlepaddle>=2.5.0  # Optional: for better jieba accuracy
spacy>=3.7.0  # Optional: for advanced NER
# Download language model: python -m spacy download zh_core_web_sm

# Testing dependencies
pytest>=7.4.3
pytest-asyncio>=0.21.1
pytest-cov>=4.1.0
pytest-mock>=3.12.0

# Optional dependencies
Pillow>=10.0.0  # For image processing
python-magic>=0.4.27  # For file type detection

# Performance optimization
psutil>=5.9.0  # For performance monitoring

# Logging
loguru>=0.7.3  # Modern logging with better developer experience
